# Pet Grooming SaaS — example scaffold

Demo product template only (Nuxt + FastAPI shape). No proprietary business logic.

- `apps/web` — Nuxt frontend placeholder
- `packages/api` — FastAPI stub
- `docker-compose.yml` — local stack stub

TODO: Replace with generated MVP when workflows are implemented.
