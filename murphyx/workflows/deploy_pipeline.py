"""
Deploy pipeline workflow — Docker/CI hooks only.

Scaffold; no cloud provider secrets in repo.
"""

def run_deploy_pipeline(ref: str) -> dict:
    """Trigger deploy — stub."""
    raise NotImplementedError
