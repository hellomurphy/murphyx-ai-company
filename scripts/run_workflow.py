#!/usr/bin/env python3
"""
CLI — run a named workflow (scaffold).

TODO: argparse workflow name + goal; call orchestrator + queue.
"""

if __name__ == "__main__":
    print("run_workflow — scaffold only; implementation TBD.")
